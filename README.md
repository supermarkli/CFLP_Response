# 审稿回复

本仓库包含了对稿件《基于可信执行环境的联邦学习平台》（稿号：20250204）的审稿意见回复。

本文档使用 [LaTeX Template for Review Responses](https://github.com/klb2/review-response-template) 模板编写，特此感谢。

## 编译

本项目包含中文内容，需要使用 `xelatex` 引擎进行编译。主文件为 `review_response.tex`。

编译指令如下：
```sh
latexmk -xelatex review_response.tex
```